<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= recipe.title %>
    </title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <nav class="navbar">
        <div class="nav-container">
            <a href="/recipes" class="logo">MyRecipe</a>
            <div class="nav-links">
                <a href="/recipes">Home</a>
                <a href="/recipes/add">Add recipe</a>
                <% if (isAuthenticated) { %>
                    <div class="profile-dropdown">
                        <button class="profile-btn" onclick="toggleDropdown(event)">
                            <span class="profile-username">
                                <%= username %>
                            </span>
                            <svg height="20" width="20" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd"
                                    d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z"
                                    clip-rule="evenodd" />
                            </svg>
                        </button>
                        <div class="dropdown-content" id="profileDropdown">
                            <a href="/logout">Logout</a>
                        </div>
                    </div>
                    <% } else { %>
                        <a href="/login">Login</a>
                        <% } %>
            </div>
        </div>
    </nav>
    <div class="container">
        <div class="recipe-article"
            style="background: var(--surface-color); box-shadow: var(--shadow); border-radius: 18px; padding: 40px 32px; margin: 32px 0;">
            <% if (recipe.image) { %>
                <img src="/<%= recipe.image.replace('\\', '/') %>" alt="Recipe Image" class="recipe-image-article"
                    style="border-radius: 12px; margin-bottom: 24px;">
                <% } %>
                    <h1 style="font-size:2.2rem; font-weight:800; color:var(--primary-color); margin-bottom:10px;">
                        <%= recipe.title %>
                    </h1>
                    <p class="recipe-meta">By Admin | <%= recipe.createdAt.toLocaleDateString() %>
                    </p>
                    <div class="recipe-content" style="margin-bottom: 24px;">
                        <p style="font-size:1.15rem; color:var(--text-color); line-height:1.7;">
                            <%= recipe.content %>
                        </p>
                    </div>
                    <% if (!isAuthenticated) { %>
                        <div
                            style="background: #e0e7ff; border-radius: 10px; padding: 18px 20px; margin-bottom: 18px; text-align:center;">
                            <span style="font-size:1.1rem; color: #0d0e0f; font-weight:600;">Want to write
                                your own recipe?</span><br>
                            <a href="/login" class="hero-btn" style="margin: 12px 8px 0 0;">Login</a>
                            <a href="/signup" class="hero-btn" style="margin: 12px 0 0 0;">Sign Up</a>
                        </div>
                        <% } %>
                            <% if (isAuthenticated) { %>
                                <div class="recipe-actions">
                                    <a href="/recipes/update?updateId=<%= recipe._id %>" class="edit-btn">Edit</a>
                                    <a href="/recipes/delete/<%= recipe._id %>" class="delete-btn"
                                        onclick="return confirm('Are you sure you want to delete this recipe?');">Delete</a>
                                </div>
                                <% } %>
                                    <a href="/recipes" class="back-link">&larr; Back to recipes</a>
        </div>
    </div>
   
    <script>
        function toggleDropdown(event) {
            event.stopPropagation();
            var dropdown = document.getElementById('profileDropdown');
            dropdown.style.display = dropdown.style.display === 'block' ? 'none' : 'block';
        }
        document.addEventListener('click', function () {
            var dropdown = document.getElementById('profileDropdown');
            if (dropdown) dropdown.style.display = 'none';
        });
    </script>
</body>

</html>